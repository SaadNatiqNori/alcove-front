// Motion helpers live in src/motion.js so top-level pages and project sections
// share one reveal language. Re-exported here so existing section imports keep
// working.
export { prefersReducedMotion } from '../motion'
